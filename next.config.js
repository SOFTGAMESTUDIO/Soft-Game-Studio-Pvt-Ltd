module.exports = {
    images: {
      domains: ['example.com', 'images.unsplash.com'], // Allowed image domains
      formats: ['image/webp'], // Modern format support
      minimumCacheTTL: 60, // Cache duration
    },
  }